    // auto T = getR();
    // arma::fmat N({{2, -4, 2}, {-3, 4 - 1}, {1, 0, 0}});
    // arma::fmat P({{-1, 1}, {0, 0}, {-1, -1}});
    // arma::fmat B = R * N * P;

    // glColor3f(0, 1, 1);
    // glPointSize(5);
    // glBegin(GL_POINTS);
    // for (int i = 0; i < 100; ++i)
    // {
    //     std::cout << B.row(i) << std::endl;
    //     glVertex2f(B(i, 0), B(i, 1));
    // }
    // glEnd();
    // glFlush();